ode/src/collision_kernel.cpp
Core collision functions and data structures, plus part of the public API for geometry objects

dispatcher for the N^2 collider functions
	functions the handle collision detection and contact generation b/n X and 1+ other classes must be of type dColliderFn
	->these functions are in this file

dxGeom
	int dxGeom::AABBTest (dxGeom *o, dReal aabb[6]) { return 1; }
	
	void getBodyPosr

public API for geometry objects
	dGeomGetAABB (dxGeom *g, dReal aabb[6]) {
		dAASSERT(g);
		dAASSERT(aabb);
		g->recomputeAABB();
		memcpy (aabb, g->aabb, 6 * sizeof(dReal));
	}
	
L936	static int dCollideUserGeomWithGeom (dxGeom *o1, dxGeom *o2, int flags, dContactGeom *contact, int skip)
	//this generic collider function is called the first time that a user
	//clas tries to collide against something. it will find out the correct
	//collider function and then set the colliders array so that the correct
	//function is called directly the next time around
